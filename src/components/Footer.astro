---
import { config } from "@/lib/config";
---

<footer class="fixed bottom-0 left-0 right-0 z-50 flex justify-center items-center h-16 bg-zinc-800 text-white">
  <p>
    &copy; {new Date().getFullYear()} - <span class="font-bold">{config.title}</span>
  </p>
</footer>
