---
import SkillIcons from "@/components/Skills";
import Layout from "../layouts/Layout.astro";
import { config } from "@/lib/config";
---

<Layout title={config.title}>
  <div class="flex flex-col items-start justify-center h-full space-y-10">
    <div class="flex flex-col md:flex-row items-center justify-center space-y-5 md:space-x-5">
      <img src={config.avatar} alt={config.title} class="rounded-full w-32 h-32" />
      <div>
        <h1 class="text-3xl text-left">About Me</h1>
        <p class="text-zinc-400 text-lg">
          Hey, i'm lasse. I'm a web developer from denmark. I have been in the programming world for almost 5 years now. I
          currently educate myself to a web developer. Wich is also what spend the most of my free time on. I'm building
          websites, apis and more, i would call myself a fullstack developer.
        </p>
      </div>
    </div>

    <div>
      <h1 class="text-3xl text-left">Skills</h1>
      <p class="text-zinc-400 text-lg">
        Below you can see the skills/tools i use on a daily basis. I'm always open to learn new things and improve my skills.
      </p>

      <SkillIcons client:load />
    </div>

    <div>
      <h1 class="text-3xl text-left">Projects</h1>
      <p class="text-zinc-400 text-lg">Here are some of the projects i have been working/work on</p>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-5 my-4">
        {
          config.projects.map((p) => (
            <div class="flex flex-col bg-zinc-800 p-3 rounded-md items-center">
              <div>
                <h2 class="text-xl">{p.name}</h2>
                <p class="text-zinc-400 text-lg">{p.description}</p>
              </div>
              {p.link && (
                <a href={p.link} target="_blank" class="text-blue-500 font-bold">
                  View
                </a>
              )}
            </div>
          ))
        }
      </div>
    </div>
  </div>
</Layout>
